{
  "name": "@desafio-brbatel/server",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "author": "Julio L. Muller",
  "scripts": {
    "postinstall": "node -e \"process.env.NODE_ENV === 'production' || require('fs').existsSync('.env') || require('fs').copyFileSync('.env.example', '.env')\"",
    "typeorm": "tsnd ../node_modules/typeorm/cli.js",
    "seed": "tsnd src/database/seeds/",
    "db:sync": "npm run typeorm -- schema:sync",
    "db:sync:seed": "npm run db:sync && npm run seed",
    "db:sync:reset": "npm run typeorm -- schema:drop && npm run db:sync",
    "db:sync:reset:seed": "npm run db:sync:reset && npm run seed",
    "migrate": "npm run typeorm -- migration:run",
    "migrate:seed": "npm run migrate && npm run seed",
    "migrate:reset": "npm run typeorm -- schema:drop && npm run migrate",
    "migrate:reset:seed": "npm run migrate:reset && npm run seed",
    "dev": "tsnd src/server.ts --clear --respawn --transpile-only --ignore node_modules",
    "development": "npm run dev",
    "build": "rm -rf build/ && tsc -p",
    "prod": "node build/server.js",
    "production": "npm run prod",
    "lint": "eslint src/ --fix"
  },
  "dependencies": {
    "cnpj": "^3.2.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "faker": "^5.5.3",
    "http-status-codes": "^2.1.4",
    "morgan": "^1.10.0",
    "pg": "^8.6.0",
    "reflect-metadata": "~0.1.13",
    "typeorm": "~0.2.32",
    "yup": "^0.32.9"
  },
  "devDependencies": {
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11",
    "@types/faker": "^5.5.5",
    "@types/morgan": "^1.9.2",
    "@types/pg": "^8.6.0",
    "eslint": "^7.27.0",
    "eslint-config-lacussoft": "^2.0.1",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  }
}
